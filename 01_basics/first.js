console.log("Hello Akshat")
// Data Type-------------
/*
let = it is contianer to store a data in a veriable. Currently js use this data type
var = it is also a continer to store a data type but at that time we not use because variable can not follow a scope
const = it can be store a variable which can't be change during the program
*/

const userId = 1001
let userName = 'Akshat Gupta'
var userEmail = 'akshatgupta@gmail.com'
usercity = 'kanpur'   
let userState ;

console.log(userId)
console.table([userName, userEmail,usercity, userState]);
